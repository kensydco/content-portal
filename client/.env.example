# API calls go to same origin in production, so no URL needed
# Only used for local development when running client separately
VITE_API_BASE_URL=http://localhost:8080
VITE_APP_NAME=Content Ingest Portal
